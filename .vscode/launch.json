{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI Backend (8101)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--port", "8101"],
      "cwd": "${workspaceFolder}/backend",
      "console": "integratedTerminal",
      "python": "${command:python.interpreterPath}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/backend"
      }
    }
  ]
}
